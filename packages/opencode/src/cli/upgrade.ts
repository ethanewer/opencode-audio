// Update checks disabled — this is a fork (opencode-audio) that has diverged
// from the upstream anomalyco/opencode repository. The upstream version check
// would compare against the original package and prompt to overwrite this fork.
export async function upgrade() {}
